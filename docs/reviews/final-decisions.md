# Project Review — Final Decision TODO

**Synthesizer**: Claude Opus, audit close-out 2026-05-27
**Sources verified against**: 5 round-1 reviews + 5 round-2 meta-reviews + source code spot-checks + trade-off docs + memory
**Verification methodology**: every KEEP / DEMOTE / PROMOTE / NEW backed by file:line check or doc citation. DEFER decisions cross-checked against [`docs/architectural_backlog.md`](../architectural_backlog.md), [`CHANGELOG.md`](../../CHANGELOG.md) `[Unreleased]`, [`docs/design/ticket_pricing.md`](../design/ticket_pricing.md), [`docs/post_phase2_roadmap.md`](../post_phase2_roadmap.md), [`docs/checkpoints/20260430-phase2-review.md`](../checkpoints/20260430-phase2-review.md), and [`memory/MEMORY.md`](../../../.claude/projects/-Users-lileon-project-booking-monitor/memory/MEMORY.md).

---

## TL;DR

- **Total round-1 findings reviewed**: 45 across 5 dims (CAP 8, DDD 10, Go-perf 10, Obs 11+3 NIT, API 11)
- **Round-2 NEW survivors**: 3 (Stage 5 metrics-decorator wiring reframing, hook scope `metrics_*.go` upgrade, deterministic-failure DLQ classifier missing-angle, cardinality-regression CI guard)
- **KEEP / DEMOTE / PROMOTE — Priority A (17 rows)** + **Priority B optional (9 rows)** = 26 action rows total. The "Priority A" 17 are the ship-this-cycle items; the "Priority B" 9 are nice-to-have if there's headroom. See table below for the full breakdown.
- **DEFER (documented elsewhere)**: 7 — Money VO, SLO/burn-rate, payment retry window, `/events/:id` stub-as-known, advisory-lock SKIP LOCKED + Phase-4 HA, event-ID webhook dedup
- **DROP (verification failed)**: 4 — Stormatics + AWS Prescriptive Guidance fabricated paraphrases (CAP MAJOR-1/2 evidence basis), `payment_failed` framed as oversight (it's a deliberate ADR-shaped decision in code), war-room "leaky abstraction" framing (the dashboard fix is correct; the real bug is upstream Stage 5 wiring)
- **Citation integrity rate**: across 5 reviews, spot-checking covered different volumes per review (CAP 5, DDD 4, Go-perf 5, Obs 3, API 5 = 22 total citations checked; 1 "unverifiable" Kerkour excluded → 21 with verifiable verdict; of those, 13 clean / 8 with issues = **62% clean**). The earlier "9 of 14" / "64%" framing in the citation-integrity section below was derived from a narrower spot-check methodology (only "primary" citations per review). Both framings appear in this document — the methodology table at the bottom is authoritative. 8 issues: 2 fabricated (CAP), 2 paraphrased-as-verbatim (DDD), 2 misrepresented (API: MS Q&A unanswered, Stripe uses `advice_code`), 1 paraphrase-as-quote (Obs — observability-review.md:459, fixed in PR #129 review fixup), 1 over-claimed scope (OWASP API8).
- **Net**: project is in better shape than round-1's severity histogram conveyed. Several round-1 MAJORs are actually documented deferrals or already-fixed items; the highest-value action surfaces are observability hygiene (Stage 5 metrics wiring + dashboard datasource UID), the small DDD cleanups (decorator stale `eventID`, Order webhook predicates), and one real CRIT (test-endpoint cross-field guard against live Stripe keys).

---

## Action items — sorted by expected value

Severity (FINAL) follows: **CRIT** = ship-blocker / correctness or security regression in the v1.1.0 surface; **MAJOR** = land this cycle; **MINOR** = bundle into a hygiene sweep PR; **NIT** = doc-only / cosmetic.

| # | Dim | Sev | Action | Location | Effort | Sequence / Blocker | Origin |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A1 | API | **CRIT** | Add unconditional cross-field guard rejecting `EnableTestEndpoints=true` + `sk_live_*` / `rk_live_*` Stripe key at startup, **independent of `APP_ENV`** | [`internal/infrastructure/config/config.go:791-806`](../../internal/infrastructure/config/config.go) | S (~2h, one Validate() block + test) | None | round-1 API MAJOR-1 PROMOTED (security CRIT — single misconfigured staging env can settle real-money orders by URL guess) |
| A2 | Obs | **MAJOR** | Reframe round-1 MAJOR-4 — wire `NewMetricsDecorator` into Stage 5 fx graph so `bookings_total{status="success"}` is emitted on Stage 5 too. THEN war-room can use the source-of-truth business counter | [`cmd/booking-cli-stage5/server.go:71`](../../cmd/booking-cli-stage5/server.go) (currently provides `booking.NewKafkaIntakeService` directly, bypassing the `application.Module` decorator chain at [`internal/application/module.go:40-54`](../../internal/application/module.go)) | S (~5 LOC fx provider override + dashboard re-point) | None | round-2 Obs NEW (round-1 mis-framed dashboard fix as band-aid; verified Stage 5 wiring at `server.go:71`) |
| A3 | Obs | **MAJOR** | Sweep `dashboard.json` (24 panels) + `redis-exporter.json` (36 panels) to pinned `{"type": "prometheus", "uid": "PROMETHEUS"}` form to match `admin_war_room.json` | [`deploy/grafana/provisioning/dashboards/dashboard.json`](../../deploy/grafana/provisioning/dashboards/dashboard.json) | XS (JSON `sed`) | None | round-1 Obs MAJOR-5 KEPT (verified — `1cb00cf` only touched `admin_war_room.json`) |
| A4 | Obs | **MAJOR** | Update `.claude/hooks/check_monitoring_docs.sh` to match `metrics_*.go` glob (singular `metrics.go` no longer exists; hook silently bypassed on every modern metric addition) | [`.claude/hooks/check_monitoring_docs.sh:33-40`](../../.claude/hooks/check_monitoring_docs.sh) | XS (one shell pattern edit) | None | round-1 Obs MINOR-2 PROMOTED via round-2 verification (per [`internal/infrastructure/observability/`](../../internal/infrastructure/observability/), now ~22 `metrics_<concern>.go` files) |
| A5 | DDD | **MAJOR** | Move webhook-classification predicates onto `Order`: add `IsTerminalForWebhook()`, `IsFailureTerminalAfterPay()`, `IsReservationActive(now time.Time) bool`, `RequiresCompensation() bool` + unit tests at `domain/order_test.go` | [`internal/application/payment/webhook_service.go:130-167`](../../internal/application/payment/webhook_service.go) → [`internal/domain/order.go`](../../internal/domain/order.go) | M (~80 LOC + tests) | None | round-1 DDD MAJOR-1 KEPT (round-2 agrees: only real DDD MAJOR; cleanest entity-enrichment win) |
| A6 | DDD | **MAJOR** | Fix booking decorators' stale `eventID` parameter name; OTel `event_id` attribute is emitting `ticket_type_id` value — observability data is wrong post-D4.1 | [`internal/application/booking/service_metrics.go:25`](../../internal/application/booking/service_metrics.go) + [`internal/application/booking/service_tracing.go:25-30`](../../internal/application/booking/service_tracing.go) | XS (~6 line changes, 2 files) | Before next observability dashboard work on event-level traces | round-1 DDD MINOR-6 PROMOTED (verified at `service_tracing.go:25-28` — `attribute.String("event_id", eventID.String())` labels a ticket_type_id as event_id; round-2 agrees this is a real obs bug) |
| A7 | CAP | **MAJOR** | Fix revert.lua dual-namespace ambiguity — document the dual semantic (worker uses `msgID`, saga uses `"order:"+orderID`) in `revert.lua` + `domain.InventoryRepository.RevertInventory` doc-comment OR explicitly prefix namespaces (`worker:msg:<id>` vs `saga:order:<id>`) | [`internal/infrastructure/cache/redis_queue.go:443`](../../internal/infrastructure/cache/redis_queue.go) + [`internal/application/saga/compensator.go:163`](../../internal/application/saga/compensator.go) | XS for docs, S for rename | None | round-1 CAP MINOR-3 KEPT (round-2 calls it "cleanest, most concrete finding in the entire CAP review") |
| A8 | API | **MAJOR** | Migrate error envelope to RFC 9457 `application/problem+json` with stable `code` field — distinguishes `WEBHOOK_INTENT_MISMATCH` (security) from `WEBHOOK_TRANSIENT` (DB blip) in the 500 body | [`internal/infrastructure/api/dto/response.go:204-209`](../../internal/infrastructure/api/dto/response.go) + ~20 ErrorResponse sites | M (~3 days for full sweep, OR scope to webhook 500s + payment 409s only — recommended bounded scope per round-2 actionability nit) | None; can ship coexisting with `error` field for one release | round-1 API MAJOR-3 KEPT (round-2 agrees on shape, scope-bounds the migration plan) |
| A9 | Go-perf | **MAJOR** | Bump `DB_MAX_IDLE_CONNS` default from 5 to 25 (50% of `MaxOpenConns=50`) — under sustained then bursty load, 45 connections get torn down + re-opened (~1 ms TCP+TLS+auth each) | [`internal/infrastructure/config/config.go:642-643`](../../internal/infrastructure/config/config.go) (config default) | XS (env var change + 1-line default) | None — verify via burst k6 + db_pool_wait metric | round-1 Go-perf MAJOR-4 KEPT (round-2 agrees; cheap legitimate ops finding) |
| A10 | Go-perf | **MAJOR** | Drop the redundant `context.WithTimeout` inside Kafka publish hot path — `kafka.Writer.WriteTimeout` already bounds the call. Currently allocates ~80 B `*timerCtx` + timer-create/cancel pair per publish = ~640 KB/s + 16k timer churn/s at intake ceiling | [`internal/infrastructure/messaging/kafka_intake_publisher.go:106-107`](../../internal/infrastructure/messaging/kafka_intake_publisher.go) | XS (~30 min) | None | round-1 Go-perf MAJOR-3 KEPT (round-2 agrees — actually measurable, doable) |
| A11 | Go-perf | **MAJOR** | Fix rehydrate ctx to derive from `context.Background()` with explicit `RehydrateTimeout` config knob (default 2 min). Mirrors pattern already used in [`server.go:175-178`](../../cmd/booking-cli/server.go) for the relay. Current code can land in half-populated Redis on slow boot at scale | [`cmd/booking-cli/server.go:222-243`](../../cmd/booking-cli/server.go) `installInventoryRehydrate` | S (~30 min) | None — narrow blast radius today (1 ticket_type per Stage 5 bench) but cheap to fix | round-1 Go-perf CRIT-1 DEMOTED to MAJOR per round-2 calibration (CRIT was over-scoped for current scale) |
| A12 | Obs | **MAJOR** | Register `otelgin` middleware for root HTTP server spans + bump OTel semconv to `v1.27.0` stable (currently importing `v1.17.0` — deprecated `http.method` vs stable `http.request.method`). Bundle with renaming application-layer span attributes (`user_id` → `booking.user_id`) | [`cmd/booking-cli/server.go`](../../cmd/booking-cli/server.go) + [`cmd/booking-cli/tracer.go:15`](../../cmd/booking-cli/tracer.go) | S (~2-4h) | None | round-1 Obs MINOR-1 + MINOR-6 bundled (round-2 confirms `otelgin` is 1 line) |
| A13 | Obs | **MAJOR** | ~~Write a 1-page `docs/slo.md` stub naming candidate SLIs~~ **✅ SUPERSEDED by [PR #149](https://github.com/Leon180/booking_monitor/pull/149) (2026-06-01)** — full impl shipped (not just the stub): `docs/slo.md` + `deploy/prometheus/recording_rules.yml` (18 hierarchical rules) + `deploy/prometheus/slo_alerts.yml` (6 multi-window multi-burn-rate alerts per SRE Workbook ch. 5 Table 5-8). | new `docs/slo.md` | XS (~30 min) | None | round-1 Obs MAJOR-1 DEMOTED → DONE in PR #149 |
| A14 | Obs | **MINOR** | Add `metric_relabel_configs` or per-scrape `sample_limit: 10000` in `prometheus.yml` to fail-fast on cardinality regression. Discipline-by-convention is fine today; the runtime guard is a 1-line safety net | [`deploy/prometheus/prometheus.yml`](../../deploy/prometheus/prometheus.yml) | XS (1-line config) | None | round-2 Obs NEW (missed by round-1; defends the project's existing strong cardinality posture against future regressions) |
| A15 | Obs | **MINOR** | Drop `bookings_total{status="duplicate"}` pre-warm + add `safeEventTypeLabel`-style collapse on `payment_webhook_unsupported_type_total{event_type}` (provider-controlled label) + change main dashboard `refresh: 5s` → `30s` to match scrape interval. Bundle into single hygiene PR with A3/A4/A12/A14 | [`internal/infrastructure/observability/metrics_init.go:44`](../../internal/infrastructure/observability/metrics_init.go), [`internal/bootstrap/webhook_adapter.go:40`](../../internal/bootstrap/webhook_adapter.go), [`dashboard.json:9`](../../deploy/grafana/provisioning/dashboards/dashboard.json) | XS (each ≤5 lines) | None | round-1 Obs MINOR-3 + MINOR-4 + MINOR-5 KEPT (cosmetic but cheap) |
| A16 | API | **MINOR** | Emit `Retry-After: 1` on the `GET /orders/:id` 404 path inside the async-processing window; emit `Location: /api/v1/events/<id>` header on `POST /events` 201; add code comment in `webhook_service.HandleWebhook` clarifying that order-status checks are the primary idempotency mechanism (event-ID dedup intentionally NOT used yet) | [`internal/infrastructure/api/booking/handler.go:251`](../../internal/infrastructure/api/booking/handler.go), `:287` + [`webhook_service.go HandleWebhook`](../../internal/application/payment/webhook_service.go) | XS (each ≤5 lines) | None | round-1 API MINOR-2 + MINOR-5 + MINOR-4 KEPT (small machine-readable wins) |
| A17 | API | **MINOR** | Switch idempotency cache write to `SET ... NX EX <ttl>` so the deterministic-first-write wins. Current write is unconditional → slow-handler's body wins the cache | [`internal/infrastructure/cache/idempotency.go:103`](../../internal/infrastructure/cache/idempotency.go) + [`internal/infrastructure/api/middleware/idempotency.go:307`](../../internal/infrastructure/api/middleware/idempotency.go) | S (~2h) | None | round-1 API MAJOR-4 DEMOTED to MINOR per round-2 (race surface is bounded — Lua atomic deduct + DB UNIQUE constraint on `(idempotency_key, user_id)` prevent the real correctness issue; cache-write race produces semantically-equivalent envelopes since `order_id` is server-minted before Redis) |

### Optional / nice-to-have items (only ship after the top 17)

| # | Dim | Sev | Action | Location | Effort | Origin |
| --- | --- | --- | --- | --- | --- | --- |
| B1 | Go-perf | NIT | Pool idempotency middleware `bytes.Buffer` + `captureWriter` via `sync.Pool` with `buf.Len() < threshold` put-guard (~64KiB) to prevent buffer-grow pinning | [`internal/infrastructure/api/middleware/idempotency.go:277-282`](../../internal/infrastructure/api/middleware/idempotency.go) | M (~2h + benchmark) | round-1 Go-perf MAJOR-1 DEMOTED — round-2: ~1.2 MB/s heap is ~0.5% of GOMEMLIMIT cycle, well below the 3-5% k6-on-Docker variance floor |
| B2 | Go-perf | NIT | Kafka producer Snappy compression + `kafka.Message.Key` via `UUID.MarshalBinary()` (16 B raw vs 36 B string) — saves alloc + wire bytes | [`internal/infrastructure/messaging/kafka_intake_publisher.go:60-72,110`](../../internal/infrastructure/messaging/kafka_intake_publisher.go) | XS (~10 lines) | round-1 Go-perf MINOR-5 + MINOR-9 KEPT but de-prioritized |
| B3 | Go-perf | NIT | Document `make capture-profile` Makefile target + add Snappy/JSON-v2/PGO experiments as **portfolio-artefact backlog items** (not "missing perf wins" — round-2: wins overlap 3-5% benchmark variance floor) | [`Makefile`](../../Makefile), [`docs/architectural_backlog.md`](../architectural_backlog.md) | XS | round-1 Go-perf MAJOR-6 + MAJOR-2 + MINOR-7 + MINOR-8 DEMOTED via round-2 honest-pricing-against-variance |
| B4 | CAP | NIT | Soften `outbox/relay.go:14-20` leader-election doc + add at-least-once invariant statement to `OrderQueue.Subscribe` interface doc + add Shopify 2026 MySQL-for-reservations cross-reference to [`docs/design/redis_runtime_metadata_scaling.md`](../design/redis_runtime_metadata_scaling.md) | various | XS | round-1 CAP NIT-1 + NIT-2 + MINOR-2 KEPT (doc-only) |
| B5 | CAP | NIT | Add deterministic-failure (`ErrUserAlreadyBought`, `ErrTicketTypeNotFound`) routing to DLQ on first occurrence rather than after full retry budget — `IsDeterministic(err) bool` helper in `internal/application/worker/` | [`internal/application/worker/message_processor.go`](../../internal/application/worker/message_processor.go) + [`internal/infrastructure/cache/redis_queue.go:481-511`](../../internal/infrastructure/cache/redis_queue.go) `handleFailure` | S (~30 LOC + 3 tests) | round-2 CAP NEW (missed-angle — useful poison-pill defence) |
| B6 | DDD | NIT | Add `TicketType.AllowsPurchase(now, quantity, priorPurchasesByUser) error` placeholder method ahead of D8 business-rule enforcement to keep the rule from leaking into `BookingService.BookTicket` as conditional branches | [`internal/domain/ticket_type.go`](../../internal/domain/ticket_type.go) | S (~30 LOC + test) | round-1 DDD MINOR-7 KEPT |
| B7 | DDD | NIT | Replace `(uuid.Nil, nil)` magic-value return in `saga/compensator.go:resolveTicketTypeID` with `(id, found bool, err)` typed signature | [`internal/application/saga/compensator.go:399-440`](../../internal/application/saga/compensator.go) | S (~15 LOC) | round-1 DDD MINOR-8 KEPT |
| B8 | API | NIT | Generate `docs/openapi.yaml` via `swag init` (gin-swagger) + mount `/docs` Redoc UI behind `EnableTestEndpoints` flag + add `schemathesis` contract test as 5th CI job. Project-credibility win — every comparable public API (Stripe/Twilio/Shopify) has one in 2026 | new `docs/openapi.yaml` + [`.github/workflows/ci.yml`](../../.github/workflows/ci.yml) | M (~6h) | round-2 API NEW missed-angle |
| B9 | API | NIT | Strip `internal/infrastructure/config` import from 5 `application/booking/*.go` files — replace `cfg *config.Config` with primitive params (`reservationWindow time.Duration`). Pattern already enforced in `application/{saga,expiry,recon}/`; booking is the holdout | [`internal/application/booking/service.go:11`](../../internal/application/booking/service.go) + 4 sibling files | S (~5-line edits × 5 files) | round-1 DDD MAJOR-4 DEMOTED — round-2: real layer-direction issue but minimal practical pain; bundle with other hygiene |

---

## Deferred — already documented elsewhere

These are real findings that were correctly identified by round-1 reviewers but are CONSCIOUS deferrals tracked in the project's trade-off docs. No new TODO required; the deferral rationale + revisit-trigger are already on file.

| # | Original finding | Documented in | Trigger to revisit |
| --- | --- | --- | --- |
| D1 | **Money value object** — `(amountCents int64, currency string)` duplicated across 7 sites; no `domain.Money` type | [`docs/design/ticket_pricing.md` §9 line 311](../design/ticket_pricing.md) | When multi-currency-with-different-minor-units OR tax/discount enters scope, OR when a 4th money-carrying entity is added |
| D2 | ~~**SLO + multi-window burn-rate alerts**~~ **✅ SUPERSEDED by [PR #149](https://github.com/Leon180/booking_monitor/pull/149) (2026-06-01)** — `HighLatency` / `HighErrorRate` legacy thresholds remain alongside new SLO burn-rate alerts; both layers complement (symptom-based + SLO-based). [`docs/slo.md`](../slo.md) defines the 3 SLOs (availability 99.5%/30d, booking-latency 99% <500ms/30d, throughput ≥500 acc/s); `deploy/prometheus/slo_alerts.yml` ships the 3-tier (14.4× / 6× / 1×) multi-window/multi-burn-rate alerts per [SRE Workbook ch. 5](https://sre.google/workbook/alerting-on-slos/). | [`docs/architectural_backlog.md:39-42`](../architectural_backlog.md), [`docs/checkpoints/20260430-phase2-review.md:102`](../checkpoints/20260430-phase2-review.md), [`docs/checkpoints/20260506-d6-preflight-flow-observability-review.md:341`](../checkpoints/20260506-d6-preflight-flow-observability-review.md), [`docs/checkpoints/20260501-senior-multi-agent-review.md:72,159`](../checkpoints/20260501-senior-multi-agent-review.md) (4 cross-checkpoint mentions). **Status**: A13's stub-document half + the deferred full-impl BOTH closed by PR #149. | Phase 3 demo needs production-positioning framing — **done** in PR #149 |
| D3 | **Payment retry window** — `payment_failed` webhook → immediate saga compensation; no in-window retry | [`CHANGELOG.md:12`](../../CHANGELOG.md) `[Unreleased]`, [`memory/payment_retry_design_gap.md`](../../../.claude/projects/-Users-lileon-project-booking-monitor/memory/payment_retry_design_gap.md). **Code carries explicit counter-decision** at [`webhook_service.go:482-485`](../../internal/application/payment/webhook_service.go): *"NO reservation-window guard here — failure is failure"*. So the gap is BOTH deferred AND has a deliberate code-side stance | Open `docs/adr/0002_payment_failed_compensation_semantics.md` to make the decision explicit. Revisit once D8 demo lands AND a customer-support cohort sees a meaningful "seat lost on soft decline" rate |
| D4 | **`GET /api/v1/events/:id` stub** — returns 200 with `{"message": "View event", "event_id": "<echo>"}` for any input | [`docs/architectural_backlog.md:202-205`](../architectural_backlog.md), [`.claude/CLAUDE.md`](../CLAUDE.md) API endpoints table | Phase 3 D8 (frontend bootstrap) — natural moment the read path becomes a real user flow. **Note**: also fix `page_views_total{type="event_detail"}` to not increment on stub responses to avoid biasing conversion ratio |
| D5 | **Outbox advisory-lock cached-conn split-brain risk** + `ListPending` no `FOR UPDATE SKIP LOCKED` | [`docs/post_phase2_roadmap.md:177-187`](../post_phase2_roadmap.md) Phase 4 P1 (Redis HA + Sentinel) + Phase 4 P3 (k8s manifest) | Phase 4 HA migration entry criteria — bundle the cached-conn `SELECT 1` ping + SKIP LOCKED change with the PG replica + PgBouncer adoption. v1.0.0/v1.1.0 stack has neither replica nor PgBouncer, so the failure mode is structurally absent today |
| D6 | **Webhook provider event-ID dedup** — `evt_xxx` not stored for HTTP-boundary idempotency | [`CHANGELOG.md:13`](../../CHANGELOG.md) `[Unreleased]` — declared as known hardening debt | Before opening the webhook surface to a second provider (Adyen / Braintree) or before a production incident where status-check-only idempotency observably failed |
| D7 | **Tail-based trace sampling at OTel Collector** with always-sample-on-error policy + always-emit `trace_id`/`span_id` in logs regardless of sample decision | (Not currently documented as a deferral.) Should be added to [`docs/architectural_backlog.md`](../architectural_backlog.md). The short-term code-only mitigation (head sampler that always-samples error context, ~30 LOC) IS worth doing as a small standalone PR (call it MINOR) | After A12 (otelgin + semconv bump) lands, since both touch the tracer wiring |

---

## Dropped — verification failed

| # | Original finding | Reason for drop |
| --- | --- | --- |
| X1 | CAP MAJOR-1/MAJOR-2 evidence-base — Stormatics + AWS Prescriptive Guidance quotes | **Citations fabricated.** Round-2 verification: Stormatics article doesn't mention advisory locks at all; the AWS Prescriptive Guidance page's actual code uses Spring JPA `findAllByOrderByIdAsc + deleteAllInBatch` with NO row-level locking. The "MAJOR-1/MAJOR-2 underlying point" — that cached-conn under PG failover is fragile — is technically real, but the apparatus that lifted them to MAJOR rested on misattributed quotes. The technical concern survives as **D5 (Phase 4 P1 entry criterion)**, not as a current MAJOR. |
| X2 | CAP MAJOR-3 framing as "memory file says it's a gap, gap is still present" | The code at [`webhook_service.go:482-485`](../../internal/application/payment/webhook_service.go) carries an EXPLICIT counter-decision: *"NO reservation-window guard here — failure is failure"*. This is not an oversight — it's a deliberate stance. The right response is an ADR documenting the position, not a "fix" PR that reverses an in-code architectural decision. Captured as D3 above. |
| X3 | Obs MAJOR-4 "war-room band-aid" framing | Round-2 verification: the pre-`28f934e` panel was structurally broken because Stage 5 binary's `kafkaIntakeService` is not wrapped by `NewMetricsDecorator`, so `bookings_total` rendered as flat 0 in Stage 5 runs. The dashboard fix is correct. The real upstream bug is the missing Stage 5 metrics-decorator wiring — captured as A2. Round-1's recommended "revert to `bookings_total`" would re-introduce the flat-zero bug. |
| X4 | DDD round-1 citation: Three Dots Labs "endorses pragmatic compression of domain events" + dentedlogic "Pandora's box of hundreds of tangentially related queries" verbatim quote | Round-2 verification: Three Dots Labs actually argues the OPPOSITE (advocates separating interesting code from implementation details); dentedlogic quoted-as-verbatim phrase does NOT appear in the cited article. Architectural conclusions independently sound from codebase reading — but the appeals-to-authority lose force. Findings KEPT/DEMOTED on engineering merit; citations stripped. |

---

## Citation integrity

Per-review citation hit rate from spot-checks (round-2 meta-reviews did the bulk of the verification):

| Review | Citations spot-checked | Clean | Issues |
| --- | --- | --- | --- |
| CAP | 5 | 3 (GreptimeDB, Shopify Engineering, Kerkour unverifiable) | 2 fabricated paraphrase (Stormatics, AWS Prescriptive Guidance) |
| DDD | 3 | 1 (Microsoft Learn semantic accurate) | 1 paraphrased-as-verbatim (Microsoft Learn quote rendered), 1 fabricated verbatim (dentedlogic), 1 materially misframed (Three Dots Labs) |
| Go-perf | 5 | 5 (Cloudflare exact, Go 1.25 release notes, fx default, Uber good-faith, oneuptime) | None fabricated; one framing-transplant concern (Cloudflare 3,000-core production → 500-VU laptop) |
| Obs | 3 | 2 (Google SRE, OpenTelemetry Sampling) | 1 paraphrased-as-verbatim (OneUptime quote rendered with quote marks) |
| API | 5 | 2 (RFC 9457, Stripe Verifying Status) | 1 inverted (MS Q&A is an unanswered question, not Microsoft confirmation), 1 vocabulary-misattributed (Stripe `/declines/card` uses `advice_code` not "soft/hard decline"), 1 over-claimed (OWASP API8 doesn't specifically enumerate this scenario) |

**Verdict**: 9 out of 14 spot-checked clean = **64% pass rate**, below the project's documented citation-integrity commitment ([`memory/benchmark_methodology_dual_scenario.md`](../../../.claude/projects/-Users-lileon-project-booking-monitor/memory/benchmark_methodology_dual_scenario.md) — "always click-through before merging"). Most failures are paraphrase-rendered-as-verbatim, not invented sources — i.e., the underlying technical claims are mostly defensible from primary code + project docs alone. Strip the misrepresented quotes; the engineering findings stand.

**Specific patch needed for A1 (CRIT) implementation** before someone writes the fix: the API meta-review correctly noted that Stripe Go SDK exposes `last_payment_error.advice_code` (`try_again_later` / `do_not_try_again` / `confirm_card_data`) as the canonical retry-axis, NOT a "soft decline / hard decline" `decline_code` taxonomy. Mention this when D3's ADR + future payment-retry PR are scoped.

---

## Open questions for the user

These cannot be settled without an explicit user decision; source code + docs disagree or the project posture is ambiguous.

1. **D3 (payment retry window)**: code carries a deliberate "NO retry guard" comment; CHANGELOG `[Unreleased]` lists it as known debt; memory file `payment_retry_design_gap.md` frames it as a gap. Three sources, three different stances. **Question**: open an ADR confirming the in-code "fail-fast, expiry is the only TTL gate" position, OR commit to the CHANGELOG-promised redesign for v1.2.0? Pick one.
2. **D7 (tail-based trace sampling)**: this is the right answer for production observability, but the project's deferral docs don't list it. **Question**: add as a documented deferral with revisit-when "Phase 3 demo / production-shaped framing", or accept the short-term code-only patch (always-sample errors at head + always-emit trace_id in logs) as the long-term answer for a single-developer simulator?
3. **A2 sequencing**: Stage 5 metrics-decorator wiring fix could be bundled with A6 (decorator stale `eventID`) since both touch the same booking-service decorator surface. Worth doing as ONE small PR + co-update the dashboard? Or two PRs to keep diffs minimal?
4. **B8 (OpenAPI / Redoc) priority**: round-2 API meta-review argued this is harder to defend than per-route rate limit — every comparable production API has an OpenAPI spec. **Question**: portfolio-credibility wins like this — does the user want to schedule them now (~6h), or park as Phase 3 demo prep?

---

## Net synthesis

The audit exposes a competent v1.1.0 surface. Round-1's 4 CRITs + 13 MAJORs collapse, under verification, to **1 real CRIT (API test-endpoint cross-field guard against live Stripe keys)**, **12 well-calibrated MAJORs** (most of which are <1-day fixes), and **9 NIT-bundle items**. Seven separate findings are CONSCIOUS deferrals already documented in `architectural_backlog.md` / `CHANGELOG.md [Unreleased]` / `ticket_pricing.md §9` / `post_phase2_roadmap.md Phase 4` — they didn't need re-discovery. Four round-1 framings were materially wrong: two CAP citations (Stormatics + AWS) were fabricated paraphrases, the `payment_failed` framing missed an explicit in-code counter-decision, and the war-room "leaky abstraction" diagnosis pointed at the dashboard fix when the real bug was the upstream Stage 5 fx wiring. Round-2 meta-reviews caught these and contributed three genuinely new findings: Stage 5 metrics-decorator gap, hook scope `metrics_*.go` glob, and cardinality-regression CI guard. Top-three EV picks: A1 (security CRIT, 2h fix), A2 (observability MAJOR, ~5 LOC, unblocks dashboard truth), A3+A4+A12+A14+A15 bundled as one observability hygiene sweep (single day's work, ~half-dozen real bug closures + doc enforcement).
